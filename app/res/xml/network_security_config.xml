<?xml version="1.0" encoding="utf-8"?>

<!--
SPDX-FileCopyrightText: 2019 Mitja Cotic <mitja_cotic@yahoo.com>

SPDX-License-Identifier: GPL-2.0-only OR GPL-3.0-only OR LicenseRef-KDE-Accepted-GPL
-->

<network-security-config>
    <!--
        Allow cleartext traffic for LAN device communication.

        COSMIC Connect communicates exclusively with devices on the local network:
        - UDP discovery broadcasts (port 1816)
        - WebSocket signaling for WebRTC Extended Display (ws://)
        - The main CConnect protocol connection is TLS-encrypted separately
        - WebRTC media streams are encrypted via DTLS

        Android's domain-config does not support IP ranges, so we permit
        cleartext globally. This is safe because the app only connects to
        paired devices on the local network.
    -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
